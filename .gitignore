__pycache__/
.pytest_cache/
*.pyc
.venv/
.env
.scratch/
.verify/
node_modules/
.next/
dashboard/.env.local
*.tsbuildinfo

# Staging area for the Hugging Face Space deploy target — a separate git repo pushed
# to a separate remote, not part of this submission. See DEPLOYMENT.md. Regenerated
# by scripts/sync_hf_space.sh, so nothing here is lost by not tracking it.
hf-space/

# The working store, rebuilt by the simulator on demand.
/triage.db

# The two 8000-payment evaluation runs ARE the results and are committed, so a fresh
# clone can serve them without a 40-minute regeneration. The 40 000-payment training
# population is 12 MB and reproduces exactly from one command:
#   python -m eval.run_arms --seed 7 --scenario normal --n 40000 --arms baseline
eval/runs/run_1NXsztG1.db
eval/model/verification.txt

# Where the dashboard reads from. Point at the deployed Render service in production.
#
# Render's free tier sleeps after 15 minutes idle and takes ~50s to wake. The dashboard
# fires GET /health on first load and shows a "waking the API" state rather than
# appearing broken while that happens.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
